meta {
  name: create prompt
  type: http
  seq: 1
}

post {
  url: {{host}}/api/prompt
  body: json
  auth: inherit
}

body:json {
  {
    "title": "Naturo",
    "prompt": "Crie o naruto"
  }
}

settings {
  encodeUrl: true
}
